# nginx-proxy includes this in the location block (default_location).
# Avatars are 5MB. Log SSE needs buffering off and a long read timeout.
client_max_body_size 6m;
proxy_buffering off;
proxy_read_timeout 3600s;
